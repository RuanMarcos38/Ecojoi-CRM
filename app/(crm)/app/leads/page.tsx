'use client';
import { useEffect,useState } from 'react';
type Lead={id:string;name:string;email?:string;phone?:string;source?:string;created_at:string};
export default function Leads(){const [rows,setRows]=useState<Lead[]>([]);useEffect(()=>{fetch('/api/contacts?status=lead',{cache:'no-store'}).then(r=>r.json()).then(d=>setRows(d.data??[]))},[]);return <div className="content"><h1 className="page-title">Leads</h1><p className="page-sub">Oportunidades capturadas e qualificadas a partir da base real.</p><div className="card table-wrap"><table className="table"><thead><tr><th>Lead</th><th>E-mail</th><th>Telefone</th><th>Origem</th><th>Criado em</th></tr></thead><tbody>{rows.length?rows.map(r=><tr key={r.id}><td><strong>{r.name}</strong></td><td>{r.email??'-'}</td><td>{r.phone??'-'}</td><td>{r.source??'-'}</td><td>{new Date(r.created_at).toLocaleDateString('pt-BR')}</td></tr>):<tr><td colSpan={5} className="empty">Nenhum lead no momento.</td></tr>}</tbody></table></div></div>}
