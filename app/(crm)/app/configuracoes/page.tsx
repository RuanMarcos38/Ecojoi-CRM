'use client';
import { FormEvent,useEffect,useState } from 'react';
import type { Permission } from '@/lib/auth/permissions';
type Flag={feature_name:string;enabled:boolean};type Me={companyName:string;companySlug:string;permissions:Permission[]};type Company={name:string;slug:string;legal_name?:string;document?:string;phone?:string;email?:string;timezone?:string;locale?:string};
const defaults=['atendimento','automacoes','relatorios','ai_agent','whatsapp','instagram','facebook'];
export default function Config(){const [flags,setFlags]=useState<Flag[]>([]);const [me,setMe]=useState<Me|null>(null);const [company,setCompany]=useState<Company|null>(null);const [error,setError]=useState('');const [saved,setSaved]=useState('');async function load(){const [fr,mr,cr]=await Promise.all([fetch('/api/features',{cache:'no-store'}),fetch('/api/me',{cache:'no-store'}),fetch('/api/settings/company',{cache:'no-store'})]);const [fd,md,cd]=await Promise.all([fr.json(),mr.json(),cr.json()]);if(fr.ok)setFlags(fd.data??[]);if(mr.ok)setMe(md.data??null);if(cr.ok)setCompany(cd.data??null)}useEffect(()=>{load()},[]);function enabled(name:string){return flags.find(f=>f.feature_name===name)?.enabled??false}async function toggle(name:string){setError('');const r=await fetch('/api/features',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({feature_name:name,enabled:!enabled(name)})});if(!r.ok){setError('Seu perfil pode visualizar, mas não alterar feature flags.');return}await load()}async function saveCompany(e:FormEvent<HTMLFormElement>){e.preventDefault();setError('');setSaved('');const fd=new FormData(e.currentTarget);const raw=Object.fromEntries(fd.entries());const body=Object.fromEntries(Object.entries(raw).map(([k,v])=>[k,String(v).trim()||null]));const r=await fetch('/api/settings/company',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify(body)});if(!r.ok){setError('Seu perfil não possui permissão para alterar os dados da empresa.');return}setSaved('Dados da empresa atualizados.');await load()}const canManage=me?.permissions.includes('features.manage')??false;const canSettings=me?.permissions.includes('settings.manage')??false;return <div className="content"><h1 className="page-title">Configurações</h1><p className="page-sub">Administração da empresa, integrações e recursos disponíveis.</p>{error&&<div className="error">{error}</div>}{saved&&<div className="success">{saved}</div>}<div className="grid settings-grid"><section className="card section"><h3>Dados da empresa</h3>{company&&<form onSubmit={saveCompany}><div className="field"><label>Nome da empresa</label><input className="input" name="name" defaultValue={company.name} required disabled={!canSettings}/></div><div className="form-grid settings-form"><div className="field"><label>Razão social</label><input className="input" name="legal_name" defaultValue={company.legal_name??''} disabled={!canSettings}/></div><div className="field"><label>Documento</label><input className="input" name="document" defaultValue={company.document??''} disabled={!canSettings}/></div><div className="field"><label>Telefone</label><input className="input" name="phone" defaultValue={company.phone??''} disabled={!canSettings}/></div><div className="field"><label>E-mail</label><input className="input" type="email" name="email" defaultValue={company.email??''} disabled={!canSettings}/></div></div><div className="setting-row"><span>Tenant</span><code>{company.slug}</code></div>{canSettings&&<button className="btn btn-primary" style={{marginTop:14}}>Salvar dados</button>}</form>}<hr/><h3>Segurança</h3><p className="muted">Tenant scoping no backend, RLS no PostgreSQL, RBAC por ação e proteção contra IDOR.</p><p className="muted">O frontend nunca define o tenant usado pelas APIs.</p></section><section className="card section"><h3>Feature Flags</h3><p className="muted">Recursos desativados ficam ocultos no menu e permanecem bloqueados no backend.</p>{defaults.map(f=><div className="activity" key={f}><div className="dot"/><div style={{flex:1}}><strong>{f}</strong><div className="muted" style={{fontSize:12}}>Ativação específica por empresa</div></div><button disabled={!canManage} className={`btn ${enabled(f)?'btn-primary':'btn-secondary'}`} onClick={()=>toggle(f)}>{enabled(f)?'Ativa':'Inativa'}</button></div>)}</section></div></div>}