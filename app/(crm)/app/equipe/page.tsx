'use client';
import { FormEvent,useEffect,useState } from 'react';
import { Plus,X } from 'lucide-react';
type Member={id:string;full_name:string;role:string;active:boolean;created_at:string};
export default function Equipe(){const [rows,setRows]=useState<Member[]>([]);const [open,setOpen]=useState(false);const [error,setError]=useState('');async function load(){const r=await fetch('/api/team',{cache:'no-store'});const d=await r.json();if(r.ok)setRows(d.data??[])}useEffect(()=>{load()},[]);async function change(id:string,role:string){setError('');const r=await fetch(`/api/team/${id}`,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({role})});if(!r.ok)setError('Você não possui permissão para alterar este usuário.');await load()}async function invite(e:FormEvent<HTMLFormElement>){e.preventDefault();setError('');const fd=new FormData(e.currentTarget);const r=await fetch('/api/team/invite',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(fd.entries()))});const d=await r.json();if(r.ok){setOpen(false);await load();return}setError(d.error==='admin_key_not_configured'?'Convites exigem SUPABASE_SERVICE_ROLE_KEY configurada somente no servidor.':'Não foi possível enviar o convite.')}return <div className="content"><div className="page-head"><div><h1 className="page-title">Equipe e Permissões</h1><p className="page-sub">RBAC validado no backend e reforçado por RLS no banco.</p></div><button className="btn btn-primary" onClick={()=>setOpen(v=>!v)}>{open?<X size={17}/>:<Plus size={17}/>} {open?'Fechar':'Convidar usuário'}</button></div>{error&&<div className="error">{error}</div>}{open&&<form className="card section" onSubmit={invite}><div className="form-grid"><div className="field"><label>Nome</label><input className="input" name="full_name" required/></div><div className="field"><label>E-mail</label><input className="input" type="email" name="email" required/></div><div className="field"><label>Perfil</label><select className="select" name="role"><option value="user">Usuário</option><option value="manager">Gestor</option><option value="company_admin">Administrador da Empresa</option></select></div></div><button className="btn btn-primary" style={{marginTop:14}}>Enviar convite</button></form>}<div className="card table-wrap"><table className="table"><thead><tr><th>Usuário</th><th>Perfil</th><th>Status</th><th>Desde</th></tr></thead><tbody>{rows.length?rows.map(r=><tr key={r.id}><td><strong>{r.full_name}</strong></td><td><select className="select" value={r.role} onChange={e=>change(r.id,e.target.value)} disabled={r.role==='super_admin'}><option value="company_admin">Administrador da Empresa</option><option value="manager">Gestor</option><option value="user">Usuário</option>{r.role==='super_admin'&&<option value="super_admin">Super Admin</option>}</select></td><td>{r.active?'Ativo':'Inativo'}</td><td>{new Date(r.created_at).toLocaleDateString('pt-BR')}</td></tr>):<tr><td colSpan={4} className="empty">Nenhum usuário disponível.</td></tr>}</tbody></table></div></div>}