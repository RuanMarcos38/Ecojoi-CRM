export default function Automacoes(){return <div className="content"><h1 className="page-title">Automações</h1><p className="page-sub">Regras por evento com feature flags e execução segura por tenant.</p><div className="grid" style={{gridTemplateColumns:'repeat(3,minmax(0,1fr))'}}>{['Novo lead → criar tarefa','Sem resposta → follow-up','Negócio ganho → onboarding'].map((x,i)=><div className="card section" key={x}><span className="badge">{i===2?'Pausada':'Ativa'}</span><h3 style={{marginTop:12}}>{x}</h3><p className="muted">Fluxo isolado por empresa e executado apenas com feature habilitada.</p><button className="btn btn-secondary">Configurar</button></div>)}</div></div>}
